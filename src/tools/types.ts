export type CardProps = {
    title?: string,
    description: string,
    id: number
};

export type DescriptionsCardProps = {
    description: string,
    id: number
};

export type ModalProps = {
    toggle: any;
    type: string
};
